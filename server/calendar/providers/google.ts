/**
 * Google Calendar (OAuth) — not implemented yet.
 * Add a CalendarProvider implementation here when OAuth client credentials exist.
 */
import type { CalendarProvider } from './types.ts'

export const googleCalendarProviderPlaceholder: Pick<CalendarProvider, 'id'> = {
  id: 'google',
}
